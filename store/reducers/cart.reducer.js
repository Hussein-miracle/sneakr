

const INITIAL_STATE = {
  cartItems:[],
  totalAmount:0,
  orders:[],
};

const cartReducer = (state = INITIAL_STATE, action) => {
  switch (action.type) {

    default:
      return state;
  }
};

export default cartReducer;
